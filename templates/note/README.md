# Note template

Copy the entire `templates/note` folder into the appropriate location under `notes/`.

Recommended destination:

```text
notes/<subject-slug>/<note-slug>/
```

Then edit `index.qmd`.

Do not write a new note directly inside the `templates` directory.
