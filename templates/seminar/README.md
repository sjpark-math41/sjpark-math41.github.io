# Seminar template

Copy this folder to:

```text
seminars/<year>/<presentation-slug>/
```

Example:

```text
seminars/2026/large-deviations-chapter-8/
```

Then edit `index.qmd`.

Use `draft: true` while preparing the presentation. Quarto preview displays drafts locally, while the production site excludes them.
