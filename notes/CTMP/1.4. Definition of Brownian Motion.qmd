---
title: "1.4. Definition of Brownian Motion"
date: 2026-01-07
categories: [Continuous Time Markov Processes, Probability]
format:
  html:
    toc: true
    html-math-method: katex
---


## 1. Exercises

This is a sample note for site building.

::: {.callout-note icon="false"}
## Exercise 1.13
Suppose $X(t)$ is a stochastic process with stationary independent increments that satisfies $E[X(1)]=0$, $E[X(1)^2]=1$, and 
$$
X(t) \sim \sqrt{t}X(1) \quad \text{for } t \ge 0.
$$
Use the Central Limit Theorem (CLT) to show that $X(t) - X(s)$ follows $N(0, |t-s|)$ for $s,t \ge 0$.
:::

::: {.proof}
**Proof.**

Let $s < t$. Since $X$ has stationary independent increments, $X(t) - X(s)$ has the same distribution as $X(t-s)$.
By the scaling property given in the problem,
$$ 
X(t-s) \sim \sqrt{t-s}X(1). 
$$
Therefore, it suffices to show that $X(1) \sim N(0,1)$.

Fix $n \in \mathbb{N}$. We can decompose $X(1)$ into a sum of $n$ independent increments:
$$
X(1) = \sum_{k=1}^n \left( X\left(\frac{k}{n}\right) - X\left(\frac{k-1}{n}\right) \right).
$$
Let $Y_{n,k} = X(\frac{k}{n}) - X(\frac{k-1}{n})$. Due to stationary independent increments, $Y_{n,k}$ are i.i.d. variables distributed as $X(1/n)$.
Using the scaling property $X(1/n) \sim \frac{1}{\sqrt{n}}X(1)$, we have:
$$
E[Y_{n,k}] = \frac{1}{\sqrt{n}}E[X(1)] = 0, \quad \text{Var}(Y_{n,k}) = \frac{1}{n}\text{Var}(X(1)) = \frac{1}{n}.
$$
Now apply the classical CLT... (이하 생략)
:::

---

## 2. Definitions

::: {.callout-tip icon="false"}
## Definition (Markov Process)
Let $X(t)$ be a stochastic process defined on a probability space $(\Omega, \mathcal{F}, P)$...
:::